#include "server.h"





int main (int argc, char *argv[]){


	if(){	

    if (-1 == close(sockfd) || -1 == close(clientfd)) {
        perror("close");
        exit(EXIT_FAILURE);
    }
    exit(EXIT_SUCCESS);
	}


	exit(EXIT_SUCCESS);

}
